Output in °C (temperature) and % (relative humidity).

Constructor:  dht11 /name/ (/communication pin to the DHT11/);

/name/.temp();  // Gives out temperature
/name/.hum();   // Gives out humidity


Installing the library:

1. Download the header (.h) and source (.cpp) files and put them into one folder

2. In your Arduino IDE got to 'Sketch' -> 'Import library' and click 'Add library'

3. Go on the folder you put the files in and click 'OK'
